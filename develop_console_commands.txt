python3 -m venv ./env
source env/bin/activate

pip install -r req.txt


python manage.py collectstatic
python manage.py makemigrations
python manage.py migrate
python manage.py runserver

npx create-react-app frontend

cd ~/tatarby/frontend
npm start


sudo service nginx restart
dhfbhdsbh74357y437fbhdsbhf47375
sudo service supervisor start
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl status tatarby
sudo supervisorctl restart tatarby


server {
    listen 80;
    server_name 80.78.241.26;
    return 301 https://gumaonelove.ru$request_uri;
}

server{
    listen 443 ssl;
    ssl on;
    ssl_certificate /etc/ssl/gumaonelove_ru.crt;
    ssl_certificate_key /etc/ssl/gumaonelove_ru.key;
    server_name gumaonelove.ru;
    client_max_body_size 100M;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_http_version 1.1;
    gzip_types text/plain text/css application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript application/javascript;

    location /static/ {
        root /home/gumaonelove/tatarby;
        expires 30d;
    }

    location /media/ {
        root /home/gumaonelove/tatarby;
        expires 30d;
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $server_name;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}


/etc/systemd/system/dialo.service

sudo systemctl start dialo
sudo systemctl enable dialo
sudo systemctl status dialo
sudo systemctl kill dialo

sudo nano /etc/nginx/sites-available/default

server{
    listen 4004 ssl;
    ssl on;
    ssl_certificate /etc/ssl/gumaonelove_ru.crt;
    ssl_certificate_key /etc/ssl/gumaonelove_ru.key;
    server_name gumaonelove.ru;
    client_max_body_size 100M;

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/gumaonelove/by_kirill/wsgi_dialo.sock;
    }
}